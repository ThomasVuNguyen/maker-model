// Generated by CadMonkey
// Prompt: hey cadmonkey, create me a pyramid
// Generated at: 2025-11-11 05:54:25

// Realistic Pyramid - Medium Complexity
// Dimensions in mm, printable without supports

$fn = 32;  // Smooth curves for printing

module pyramid(height=50) {
    // Main pyramid body with rounded edges
    difference() {
        union() {
            // Base pyramid shape (truncated cube)
            translate([0, 0, height/2])
                cylinder(h=height*1.5, r1=height*0.9, r2=height*0.7);
            
            // Top pyramid point (cylinder)
            translate([0, 0, height])
                cylinder(h=height*1.5, r=height*0.05);
        }
        
        // Hollow out the interior
        translate([0, 0, -1])
            cylinder(h=height+2, r1=height*0.8, r2=height*0.6);
            
        // Add base fillet for realism
        translate([0, 0, -2])
            sphere(r=height*0.03);
    }
    
    // Add decorative ridges along sides
    module side_ridge(side_angle) {
        rotate([side_angle, 0, 0]) 
            translate([0, 0, height/2 + 10])
                linear_extrude(height=20, center=true)
                    polygon(points=[[0,0], [height*0.35, height*0.15], [height*0.45, height*0.25]]);
    }
    
    side_ridge(0);
    side_ridge(180);
}

// Create the complete pyramid
pyramid();