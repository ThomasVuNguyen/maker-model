// Generated by CadMonkey
// Prompt: hey cadmonkey, create me a cylinder
// Generated at: 2025-11-11 05:48:03

// Realistic Medium-Complexity Cylinder Tool (Tool Steel)
// Overall length ≈ 90 mm, printable without supports

$fn = 32; // smooth curves

module main_grip() {
    // 6-groove hexagonal grip pattern
    for(i=[0:5]) {
        rotate([0, 0, i*60])
            translate([0, -2.54, 8])
                cube([1.27, 5.12, 10], center=true);
    }
}

module tool_steel_core() {
    color("silver")
    difference() {
        union() {
            // central shaft
            cylinder(h=85, d=15);

            // six cylindrical grooves
            for(i=[0:5]) {
                rotate([0, 0, i*60 + 30])
                    translate([7.62, 0, 40])
                        cube([14.3, 2.54, 70], center=true);
            }

            // chamfered end
            translate([0, 0, 85])
                cylinder(h=5, d1=15, d2=11);
        }

        // lightening holes (subtle)
        for(i=[0:5]) {
            rotate([0, 0, i*60 + 30])
                translate([9.55, 0, 45])
                    sphere(d=4);
        }
    }
}

module heat_sink() {
    // small flat plate with cooling fins
    color("gray")
    difference() {
        // main heat sink block
        translate([-8, 0, -5])
            cube([16, 25, 10]);

        // cooling fins via subtractive operations
        for(y=[-1,-0.5,0.5,1]) {
            translate([-8, y*10, -5])
                cube([16, 1, 10]);
        }

        // central through hole
        translate([-4, 0, -5])
            cylinder(h=22, d=8);
    }
}

module allen_key_tip() {
    // short metal insert at tip
    color("gold")
    union() {
        // main shaft
        translate([0, 0, 82])
            cylinder(h=15, d=11);

        // allen key slot
        translate([0, 0, 87])
            rotate([90, 0, 0])
                cylinder(h=20, d=8);
    }
}

module end_cap() {
    // rounded copper cap
    color("copper")
    difference() {
        // main cap body
        translate([0, 0, 100])
            cylinder(h=8, d=18);

        // hollow underside
        translate([0, 0, 97])
            cylinder(h=6, d=13);

        // screw threads on bottom
        for(a=[0:120:240]) {
            rotate([0, 0, a])
                translate([7.38, 0, 98])
                    cube([1.53, 1.05, 1.53], center=true);
        }
    }
}

// Assembly
union() {
    main_grip();
    heat_sink();
    allen_key_tip();

    // lubrication groove
    color("darkblue", 0.4)
    translate([0, 0, 30])
        cylinder(h=60, d=1.50);
}