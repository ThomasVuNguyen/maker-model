// Generated by CadMonkey
// Prompt: hey cadmonkey, create me a cat
// Generated at: 2025-11-12 00:33:07

// Main body of the cat
module body() {
    // Torso
    hull() {
        translate([0, 15, 10]) sphere(d=25);
        translate([0, -5, 8]) sphere(d=20);
        translate([-10, 12, 6]) sphere(d=18);
    }
    
    // Neck and head base
    translate([0, -2, 14]) rotate([-30, 0, 0])
    union() {
        cylinder(h=12, d1=15, d2=12); // Head
        translate([0, -7, 11]) sphere(d=10); // Snout
    }
}

// Legs module
module leg(x_pos, z_offset) {
    translate([x_pos, 0, z_offset]) {
        // Upper leg
        cylinder(h=8, d1=8, d2=6);
        
        // Lower leg
        translate([0, 0, 8]) cylinder(h=9, d=6);
        
        // Paw
        translate([0, 0, 17]) sphere(d=7);
    }
}

// Tail module
module tail() {
    translate([-12, -5, 8]) {
        rotate([45, 0, 90]) {
            // Tail base
            cylinder(h=20, d1=5, d2=3);
            
            // Tail tip
            translate([0, 0, 20]) sphere(d=4);
        }
    }
}

// Ears module
module ear(x_pos) {
    translate([x_pos, -2, 22]) {
        rotate([-60, x_pos*10, 0]) {
            // Outer ear
            scale([1.5, 0.8, 1]) sphere(d=8);
            
            // Inner ear
            translate([0, 0, 2]) 
            scale([1, 0.6, 1]) sphere(d=6);
        }
    }
}

// Complete cat assembly
union() {
    color("Sienna") {
        // Main body
        body();
        
        // Legs - positioned for stability
        leg(5, 0);   // Front left
        leg(-5, 0); // Front right
        leg(8, -8); // Back left
        leg(-8, -8); // Back right
        
        // Tail
        tail();
    }
    
    // Ears (characteristic feature)
    ear(5);
    ear(-5);
}