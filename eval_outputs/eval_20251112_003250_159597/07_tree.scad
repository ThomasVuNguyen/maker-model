// Generated by CadMonkey
// Prompt: hey cadmonkey, create me a tree
// Generated at: 2025-11-12 00:36:34

// Tree model using standard OpenSCAD primitives

module trunk() {
    // Main trunk with slight taper
    cylinder(h=15, r1=2.5, r2=1.8, center=false);
}

module branch(length, angle) {
    // Branch with natural curve
    rotate([0, angle, 0]) 
        cylinder(h=length, r=0.6, center=false);
}

module leaf() {
    // Simple leaf shape
    scale([1, 0.7, 1])
        sphere(r=1.2);
}

// Main tree assembly
union() {
    // Trunk
    color("brown")
    translate([0, 0, -10])
        trunk();
    
    // Primary branches
    color("darkgreen") {
        // First set of branches
        for(i = [0:120:240]) {
            rotate([0, i, 0]) 
                translate([0, 0, 12])
                    branch(8, 30);
            
            rotate([0, i+60, 0]) 
                translate([0, 0, 14])
                    branch(6, 25);
        }
        
        // Second set of branches (higher up)
        for(j = [0:90:270]) {
            rotate([j, j*2, 0]) 
                translate([0, 0, 18])
                    branch(10, 20);
        }
        
        // Top branches
        for(k = [0:45:135]) {
            rotate([k, k*2, 0]) 
                translate([0, 0, 22])
                    branch(8, 15);
        }
    }
    
    // Leaves distributed along branches
    color("green") {
        for(l = [0:360/3:720]) {
            rotate([l, l*2, 0]) 
                translate([0, 0, 16 + l/3])
                    leaf();
        }
    }
}