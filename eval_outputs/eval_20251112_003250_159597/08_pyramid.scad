// Generated by CadMonkey
// Prompt: hey cadmonkey, create me a pyramid
// Generated at: 2025-11-12 00:36:39

// Create a regular pyramid with base dimensions and height
module pyramid(base_size=20, height=30) {
    // Calculate pyramid sides (base + height)
    side_length = base_size * sqrt(2);
    
    // Define pyramid points (vertices of a square)
    vertex_x = -side_length/2;
    vertex_y = -side_length/2;
    vertex_z = 0;
    
    // Position vertices along a circle to form pyramid
    translate([vertex_x, vertex_y, 0])
    rotate([0, 90, 0])  // Rotate to align with circle center
    linear_extrude(height=height, center=true) {  // Extrude points into 3D space
        polygon(points=[
            [0, 0],           // Bottom left corner
            [vertex_x, 0],   // Bottom right corner
            [0, side_length*sqrt(2)]  // Top right corner (square)
        ]);
    }
}

// Create the pyramid model
pyramid();