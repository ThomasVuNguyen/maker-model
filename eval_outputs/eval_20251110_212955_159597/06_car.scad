// Generated by CadMonkey
// Prompt: hey cadmonkey, create me a car
// Generated at: 2025-11-10 21:30:24

// Car Model - Simplified

$fn = 60;

module wheel(radius, width) {
    difference() {
        cylinder(r=radius, h=width);
        translate([0,0,-1]) cylinder(r=radius-2, h=width+2);
    }
}

module car_body(length, width, height) {
    cube([length, width, height]);
}


module car_wheel(radius, width) {
    rotate([90,0,0])
    translate([-radius/2, -width/2, 0])
    wheel(radius, width);
}



module car_body(length, width, height) {
    cube([length, width, height]);
}




module car(length, width, height, wheel_radius, wheel_width) {

    // Body
    car_body(length, width, height);

    // Wheels
    translate([0, -width/2, -wheel_width/2])
    rotate([90, 0, 0])
    car_wheel(wheel_radius, wheel_width);

    translate([length/2, -width/2, -wheel_width/2])
    rotate([90, 0, 0])
    car_wheel(wheel_radius, wheel_width);
    
    translate([0, length/2, -wheel_width/2])
    rotate([90, 0, 0])
    car_wheel(wheel_radius, wheel_width);


     translate([0, -width/2, -wheel_width/2])
    rotate([90, 0, 0])
    car_wheel(wheel_radius, wheel_width);



}

car(50,20,15,25,10);