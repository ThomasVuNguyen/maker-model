// Generated by CadMonkey
// Prompt: hey cadmonkey, create me a cat
// Generated at: 2025-11-10 21:30:07

// Cat Model in OpenSCAD

module cat(size = 50) {
  // Body
  cylinder(h = size * 0.6, r1 = size * 0.4, r2 = size * 0.35);

  // Head
  translate([0, size * 0.3, size * 0.6])
    sphere(r = size * 0.25);

  // Ears (Left)
  translate([-size * 0.15, size * 0.5, size * 0.9]) {
    rotate([0, -20, 0])
      cone(h = size * 0.1, r1 = size * 0.08, r2 = size * 0.02);
  }

  // Ears (Right)
  translate([size * 0.15, size * 0.5, size * 0.9]) {
    rotate([0, 20, 0])
      cone(h = size * 0.1, r1 = size * 0.08, r2 = size * 0.02);
  }
  
  // Tail
  translate([size * 0.3, -size * 0.2, size * 0.3]) {
    rotate([90, 0, 0])
      cylinder(h = size * 0.3, r1 = size * 0.05, r2 = size * 0.01);
  }
}

cat();